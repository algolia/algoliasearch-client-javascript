{
  "name": "@nunomaduro/lightsearch",
  "private": false,
  "version": "4.0.0-alpha.0",
  "license": "MIT",
  "description": "test",
  "dependencies": {
    "@algolia/cache-in-memory": "file:../cache-in-memory",
    "@algolia/cache-browser-local-storage": "file:../cache-browser-local-storage",
    "@algolia/logger-console": "file:../logger-console",
    "@algolia/logger-types": "file:../logger-types",
    "@algolia/requester-browser-xhr": "file:../requester-browser-xhr",
    "@algolia/requester-types": "file:../requester-types",
    "@algolia/transporter": "file:../transporter",
    "@algolia/transporter-types": "file:../transporter-types"
  },
  "scripts": {
    "build": "yarn build:umd",
    "build:umd": "BABEL_ENV=rollup rollup -c rollup.config.js",
    "release:clean": "rm -rf dist",
    "release:build": "mkdir -p dist && cp {package.json} dist && yarn build",
    "release:stable": "",
    "release:alpha": "yarn publish --tag alpha --dry-run --verbose --access public"
  }
}
