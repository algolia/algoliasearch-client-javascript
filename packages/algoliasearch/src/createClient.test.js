/* eslint-env mocha */
import expect from 'expect';
import sinon from 'sinon';
import createClient from './createClient.js';

describe('client()', () => {
  it('can be called', () => {
    client();
  });
});
