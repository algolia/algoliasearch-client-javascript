// todo: make this into a package with the old code
export default function deleteByQuery() {
  return new Error(
    `this isn't supported, use the replacement package: algoliasearch-delete-by-query`
  );
}
