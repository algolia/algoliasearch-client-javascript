export type TopSearch = {
  /**
   * The search query.
   */
  search: string;
  /**
   * The number of occurrences.
   */
  count: number;
  /**
   * Number of hits that the search query matched.
   */
  nbHits: number;
};
