[
  {
    "testName": "client throws with invalid parameters",
    "autoCreateClient": false,
    "steps": [
      {
        "type": "createClient",
        "parameters": {
          "apiKey": "blah"
        },
        "expected": {
          "error": "`appId` is missing."
        }
      }
    ]
  }
]
