post:
  tags:
    - search
  operationId: getObjects
  summary: Retrieve one or more objects.
  description: Retrieve one or more objects, potentially from different indices, in a single API call.
  requestBody:
    required: true
    description: The Algolia object.
    content:
      application/json:
        schema:
          title: getObjectsObject
          description: The `getObjects` requests.
          type: object
          additionalProperties: false
          properties:
            requests:
              type: array
              items:
                description: GetObject operation on an index.
                title: multipleGetObjectsObject
                type: object
                additionalProperties: false
                required:
                  - objectID
                  - indexName
                properties:
                  attributesToRetrieve:
                    type: array
                    items:
                      type: string
                    description: List of attributes to retrieve. By default, all retrievable attributes are returned.
                  objectID:
                    type: string
                    description: ID of the object within that index.
                  indexName:
                    type: string
                    description: name of the index containing the object.
  responses:
    '200':
      description: OK
      content:
        application/json:
          schema:
            title: getObjectsResponse
            type: object
            additionalProperties: false
            properties:
              results:
                type: array
                description: List of results fetched.
                items:
                  type: object
                  description: Fetched object.
    '400':
      $ref: ../../../common/responses/BadRequest.yml
    '402':
      $ref: ../../../common/responses/FeatureNotEnabled.yml
    '403':
      $ref: ../../../common/responses/MethodNotAllowed.yml
    '404':
      $ref: ../../../common/responses/IndexNotFound.yml
